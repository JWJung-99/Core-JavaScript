{
  "devDependencies": {
    "eslint": "^8.56.0",
    "live-server": "^1.2.2",
    "prettier": "^3.2.4"
  },
  "scripts": {
    "dev": "live-server client --host=localhost --port=5500 --no-browser"
  }
}
